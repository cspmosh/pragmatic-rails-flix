<%= form_with(model: [@movie, @review], local: true) do |f| %>
  <%= render "shared/errors", object: @review %>

  <%= f.label :stars %>
  <% Review::STARS.each do |s| %>
    <span class="star">
      <%= f.radio_button :stars, s %>
      <%= label_tag s %>
    </span>
  <% end %>

  <%= f.label :comment %>
  <%= f.text_area :comment, rows: 5, placeholder: "What did you think?" %>

  <%= f.submit %>
<% end %>


